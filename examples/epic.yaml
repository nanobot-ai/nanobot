agents:
  main:
    name: Something Epic
    model: gpt-4.1
    instructions: |
      ${servers.epic.instructions}
    mcpServers: epic

mcpServers:
  epic:
    url: https://epic-me-mcp.kentcdodds.workers.dev/mcp
